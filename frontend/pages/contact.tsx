import Head from 'next/head';
import Layout from 'layout';

const Contact: React.FC<void> = (): JSX.Element => {
  return (
    <Layout>
      <Head>
        <title>Contact page</title>
      </Head>
      Contact page
    </Layout>
  );
};

export default Contact;
